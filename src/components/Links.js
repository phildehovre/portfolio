import React from 'react'
import Section from './Section'

const Links = (props) => {
    return (
        <Section {...props}>
            <div></div>
        </Section>
    )
}

export default Links