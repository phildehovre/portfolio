import React from 'react'
import Section from './Section'

const About = (props) => {
    return (
        <Section {...props}>
            <div>About</div>
        </Section>
    )
}

export default About